node_modules/
data/
local/
.claude/worktrees/
.claude/skills/
.env*
.DS_Store
.vercel
# Transient browser-verification output agents leave behind (screenshots of the
# board can contain private card content — never let them ride along in a PR).
.playwright-mcp/
/*.png

# Perf harness scratch output (never commit)
scratch/
