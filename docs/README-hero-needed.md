# Hero screenshot needed

`site/og.png` is a social-card illustration (mascot + tagline), not an actual
screenshot of the board UI — so it isn't a fair stand-in for a hero image.

The README now references `docs/board.png` for its hero image, but that file
doesn't exist yet. Maintainer: drop a real screenshot of the board (Backlog →
Queued → Running → Review → Done, with a couple of cards in flight) at
`docs/board.png` to complete this.
